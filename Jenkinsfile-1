
pipeline {
  agent {label "linux"} 
  stages {
    stage('build') {
      steps {
        sh """
          docker build -t liquibase_v1 .
        """
      }
    }
    stage("run") {
      steps {
        sh """
          docker run --rm liquibase_v1
        """
      }
    }
  }
}