
pipeline {
  agent {label "master"} 
  stages {
    stage('build') {
      steps {
        sh """
          docker.build("--build-arg jenkinsParam=$jenkinsParam .")
        """
      }
    }
    stage("run") {
      steps {
        sh """
          docker run --rm --network="host" test_liquibase_v
        """
      }
    }
  }
}