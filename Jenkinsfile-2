pipeline {
  agent {
    docker { image 'liquibase/liquibase:4.4.2' }
  }
  stages {
    stage('Status') {
      steps {
        echo "${env.TEST_LIQUIBASE_USER}"
      }
    }
  }
  post {
    always {
      cleanWs()
    }
  }
}