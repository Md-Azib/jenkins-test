pipeline {
  agent {
    docker { image 'liquibase/liquibase:4.4.2' }
    docker { image 'python:3.5.1' }
  }
  stages {
    stage('Status') {
      steps {
        sh 'python3 prog.py'
      }
    }
  }
  post {
    always {
      cleanWs()
    }
  }
}
